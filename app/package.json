{
  "name": "cicd-demo-app",
  "version": "1.0.0",
  "description": "Sample REST API dung de minh hoa pipeline Jenkins + Harbor + Ansible",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "test": "node --test test/"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^4.19.2"
  }
}
